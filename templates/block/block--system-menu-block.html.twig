{% do attributes.removeAttribute('id') %}
<nav{{ attributes.addClass(class) }}>
  {{ title_prefix }}
  {% if label %}
    <p{{ title_attributes }}>{{ label }}</p>
  {% endif %}
  {{ title_suffix }}
  {% block content %}
    {{ content }}
  {% endblock %}
</nav>
