{% if content %}
  {% block region %}
    <footer{{ attributes.addClass(region) }}>
      {% block content %}
        <div class="{{ region ~ '__content container' }}">
          {{ content }}
        </div>
      {% endblock %}
    </footer>
  {% endblock %}
{% endif %}
